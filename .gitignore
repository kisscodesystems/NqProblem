# What the build compiles and packages. The jar of the application is a build
# artifact and no part of this repository: the builder script of your platform
# writes it here, and the jar of a released version is published as a binary of
# that release.
bin
/NqProblem.jar

# What the test run compiles and writes.
build
